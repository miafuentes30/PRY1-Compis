let ok: boolean = true;
if (ok) { print(ok); }
while (false) { break; }
do { continue; } while (false);
for (let i: integer = 0; i < 2; i = i + 1) { print(i); }
switch (ok) { case true: print(ok); default: print(false); }
